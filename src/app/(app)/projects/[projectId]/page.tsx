/**
 * One project. The board is rendered by the (app) layout from the workspace
 * already on the client, keyed off this route's `projectId`
 * (components/workspace/workspace.tsx); this route only gives it its URL.
 */
export default function ProjectPage() {
  return null;
}
