/**
 * All tasks. The board itself is rendered by the (app) layout from the
 * workspace already on the client (components/workspace/workspace.tsx); this
 * route only gives it its URL.
 */
export default function AllTasksPage() {
  return null;
}
