{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled connect page (tauri://localhost) may use the core APIs and the app's own commands, and drag the window from its `data-tauri-drag-region` elements. No `remote` entry on purpose: pages loaded from the connected server get no IPC access beyond what capabilities/remote.json grants.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
